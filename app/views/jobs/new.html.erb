<!-- <h1>New Job</h1>
<%= render 'form' %>

<%= link_to 'Back', user_job_path %>

<%= form_with(model: [ @user, @user.jobs.build ], local: true) do |form| %>
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %> -->
