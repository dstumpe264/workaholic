<h1>All Users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Password</th>
    <th></th>
  </tr>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.pwd %></td>
        <td><%= link_to 'Show', user_path(user) %></td>
      </tr>
    <% end %>
</table>
